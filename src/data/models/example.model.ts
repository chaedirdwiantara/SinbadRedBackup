/** === IMPORT EXTERNAL === */
/** === THIS FOR EXTERNAL INTERFACE === */
/** => example */
export interface Example {
  id: string;
  name: string;
  image: string;
  createdAt: string;
  updatedAt: string | null;
}
