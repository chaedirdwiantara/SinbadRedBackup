/** === IMPORT EXTERNAL === */
/** === THIS FOR EXTERNAL INTERFACE === */
/** => example */
export interface Example2 {
  id: string;
  name: string;
  image: string;
  createdAt: string;
  updatedAt: string | null;
}
