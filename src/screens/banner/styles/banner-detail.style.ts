import { StyleSheet } from 'react-native';

export const BannerDetailStyles = StyleSheet.create({
  sectionContainer: {
    marginHorizontal: 16,
    marginBottom: 16,
  },
  singleContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
