import { StyleSheet } from 'react-native';

export const CartProfileCompletionStyles = StyleSheet.create({
  modalContentContainer: {
    padding: 16,
    alignItems: 'center'
  },
  modalImageContainer: {
    marginBottom: 24
  },
  modalImage: {
    alignItems: 'center',
    justifyContent: 'center'
  }
});
