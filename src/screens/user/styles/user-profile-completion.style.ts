import { StyleSheet } from 'react-native';

export const UserProfileCompletionStyles = StyleSheet.create({
  modalContentContainer: {
    alignItems: 'center',
  },
  modalImage: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 200,
    resizeMode: 'contain',
    marginVertical: 24,
  },
});
